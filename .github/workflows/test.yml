name: "build-test"
on: [push]

jobs:
  build: # make sure build/ci work properly
    runs-on: ubuntu-latest
    steps:
    - name: "npm ci"
      run: npm ci

    - name: "npm run build"
      run: npm run build
